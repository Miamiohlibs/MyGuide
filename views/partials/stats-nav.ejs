
<% let graphActive = ''; graphAriaCurrent = ''; %>
<% if (page == 'usage') {  %>
    <% graphActive = 'active'; %>
    <% graphAriaCurrent = 'aria-current="page"'  %>
<% } %>

<% let repeatActive = ''; repeatAriaCurrent = ''; %>
<% if (page == 'repeatUsers') {  %>
    <% repeatActive = 'active'; %>
    <% repeatAriaCurrent = 'aria-current="page"'  %>
<% } %>

<% let subjectActive = ''; subjectAriaCurrent = ''; %>
<% if (page == 'subjects') {  %>
    <% subjectActive = 'active'; %>
    <% subjectAriaCurrent = 'aria-current="page"'  %>
<% } %>

<% let favoritesActive = ''; favoritesAriaCurrent = ''; %>
<% if (page == 'favorites') {  %>
    <% favoritesActive = 'active'; %>
    <% favoritesAriaCurrent = 'aria-current="page"'  %>
<% } %>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">MyGuide Usage</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link <%- graphActive %>" <%- graphAriaCurrent %> href="/stats/usage">Stats Graph</a>
          <a class="nav-link <%- repeatActive %>" <%- repeatAriaCurrent %> href="/stats/repeatUsers">Repeat Users</a>
          <a class="nav-link <%- subjectActive %>" <%- subjectAriaCurrent %> href="/stats/subjects">Subject Analysis</a>
          <a class="nav-link <%- favoritesActive %>" <%- favoritesAriaCurrent %> href="/stats/favorites">Favorites</a>
        </div>
      </div>
    </div>
  </nav>